# Type or flavor of the pipeline to be generated
flavor: konflux

# A buildpack builder project
builder:
  name: pipeline-konflux # name of the pipeline to be created
  repository:
    name: https://github.com/paketo-community/builder-ubi-base
    branch: main

