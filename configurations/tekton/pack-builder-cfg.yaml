type: tekton
name: "pack builder"
namespace:

job:
  domain: buildpack
  # One of the supported resources: PipelineRun, Pipeline, Task
  resourceType: PipelineRun
  name: pack-builder-push
  description: "This Pipeline builds a Buildpack builder image using the pack CLI."
